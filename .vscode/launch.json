{
    // Use IntelliSense to learn about possible attributes.
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/backend/cmd/movie-catalog",
            "env": {
                // The config lives at the repo root, above the Go module.
                "CONFIG_FILE": "${workspaceFolder}/application.yaml"
            },
            "args": []
        }
    ]
}
